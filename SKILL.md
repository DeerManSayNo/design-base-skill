---
name: design-system
description: 为 AI 编码项目提供可继承的设计基础设施，用于新项目建设、界面与组件迭代及设计系统治理。从产品意图推导规则，维护设计资产并验证实现效果。
---

# 设计基础设施

仅注册此入口，内部模块由当前代理按需读取并执行。项目保存可继承的设计依据、规则与资产；Skill 提供跨项目的建设、实施和验证方法，不预设视觉风格。

生成页面或方案前默认预防 AI 味：不凭熟悉的页面公式添加无产品依据的视觉、文案或交互。成熟设计系统以有效规则为主、防偏为辅，不因风格元素本身否定已有设计。

## 每次执行

1. 明确当前任务，读取项目 `AGENTS.md` 指向的设计入口，从核心理念及相关子系统规则定位所需 Token 与组件契约；入口缺失先定位已有主题、组件和设计资料。
2. 新页面或流程调整先确定主任务、信息优先级、主操作与关键状态，再选布局和组件；局部修改沿用有效决定，只处理本次影响。关键产品语义缺失才提问，可逆选择说明依据后推进。
3. 按下表执行对应模块；共享设计决定或适用范围变化须经 foundation 准入。原范围复用、已批准例外实施及不改变值/语义的机械修复只验本次影响。已有实现不自动成为规范，不为迁就组件静默放宽规范。
4. 实现或资产改动后执行 verify 的公共要求及适用专项，修正本次问题并复查，同步受影响资产。只读审查仅报告。

以上是执行要求，无须逐项汇报或另建过程文档。交付简述关键决定、验证结果与限制。

## 按需读取

| 任务 | 执行模块 | 追加条件 |
| --- | --- | --- |
| 仅输出设计方案 | 沿用项目依据；信息关系或方向未定时读设计推导，自拟视觉时追加审美防偏 | 走查任务、布局、组件与状态；新决定保持候选，不启动实现或资产建设，不将纸面走查称为运行验证 |
| 局部界面、样式或组件修改 | [compose](modules/compose/instructions.md) → [verify](modules/verify/instructions.md) | 规则缺失或冲突时进入 foundation |
| 已有体系中的新页面或流程调整 | compose → verify | 信息关系或视觉方向未解决时读 [设计推导](references/design-reasoning.md)；需要自拟视觉或检查 AI 平均化倾向时读 [审美防偏](references/anti-ai-aesthetic.md) |
| 新项目、设计升级或规则治理 | [foundation](modules/foundation/instructions.md)，涉及实现再执行 compose，最后 verify | 自拟或重选方向读设计推导与审美防偏；系统级 AI 味审查读审美防偏；确需建立资产才读模板 |
| 只读审查 | verify | 明确审查 AI 味、模板感或通用生成痕迹时追加审美防偏；发现缺口只报告依据，不启动建设 |

资料足以支持当前决定时停止扩展读取；新增缺口才追加对应章节。此停止条件不省略已选执行模块与验证。首次使用 Skill、文件中的链接都不是全文加载理由。已读取且未变化的内容不重复读取，后续只保留依据与资产引用、决定、影响范围及未决项。

## 项目资产

所有项目统一采用模块化设计资产结构，不按项目大小选择组织方式。`docs/design-system.md` 只承担总览与索引；设计依据、Token 契约及各子系统规则独立成文件，结构见 [项目模板](templates/README.md)。已有等价模块化目录可保留实际路径；单文件规范在首次维护时由 foundation 迁移并同步引用，只读审查仅报告。项目 `AGENTS.md` 引用实际入口。设计值以 Token 为准，行为由契约表达，API 看源码；组件与示例共用实现，沿用项目代码目录与工具链。项目资产始终写入当前项目，全局 Skill 只保存通用指令和模板。

首次组织资产或调整归属才读 [架构参考](references/architecture.md)；制定视觉或交互规则有缺口时读 [设计参考](references/design-spec.md)。新决定明确范围，采纳后替代该范围内旧规则；探索保持候选身份，分批迁移记录例外与剩余范围。
